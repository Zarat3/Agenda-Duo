---
description: Ativa o DeepSeek no projeto atual copiando as configs do OpenRouter do deepseek-claude
---

Execute diretamente, sem perguntar nada ao usuário:

1. Copie `/home/eliasaf/Documentos/Projetos/deepseek-claude/.claude/settings.local.json` para `.claude/settings.local.json` no projeto atual (diretório de trabalho corrente).
2. Se o diretório `.claude` não existir no projeto atual, crie-o primeiro.
3. Ao final, informe o caminho completo do arquivo copiado.
